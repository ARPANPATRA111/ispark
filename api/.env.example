# Server config
PORT=8080

# Database config
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=user
DB_NAME=isparc
DB_SSLMODE=disable

# JWT Secrets config
JWT_SECRET=your_jwt_access_token_secret_key_here
JWT_REFRESH_SECRET=your_jwt_refresh_token_secret_key_here

# Email delivery for OTPs.
# Preferred: Brevo's HTTP API (port 443). Many hosts block outbound SMTP, which
# stalls the request until the TCP timeout, so use this wherever possible.
BREVO_API_KEY=
# Must be an address verified with the provider. Used by both transports.
SMTP_SENDER=your_email@example.com

# Fallback SMTP transport, used only when BREVO_API_KEY is empty.
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your_smtp_user@example.com
SMTP_PASS=your_smtp_password
SEED_DEV_DATA=true

# --- Cloud deployment (all optional; safe to leave unset locally) ---

# Extra comma-separated CORS origins on top of the localhost defaults,
# e.g. https://your-app.vercel.app
ALLOWED_ORIGINS=

# Set true when connecting through a transaction-mode pooler
# (Supabase Supavisor/PgBouncer, port 6543) — disables prepared statements.
DB_PREFER_SIMPLE_PROTOCOL=false

# Vercel Blob storage for certificate uploads. When the token is set, files go
# to the Blob store instead of local ./uploads. Use a PRIVATE store.
BLOB_READ_WRITE_TOKEN=
BLOB_ACCESS=private
